(define s-index "index")			; link to index
(define s-manage "manage")			; link to management panel
(define s-status-index "viewing index")		; index status message
(define s-status-thread "viewing thread NUM")	; thread status message
(define s-status-manage "manager mode")		; manager status message
(define s-name "name")				; name in the post form
(define s-post "post")				; submit text in the post form
(define s-mm-no "no")
(define s-mm-re "re")
(define s-mm-name "name/ip")
(define s-mm-comment "comment")
(define s-mm-rm "rm")
(define s-mm-ban "ban")
